<div class="container">
  <div class="columns is-centered">
    <div class="column is-half is-narrow">
      <%= render "form.html", Map.put(assigns, :action, url_path(@conn, :create)) %>

      <%= if assigns[:url] do %>
        <hr />

        <div class="tile is-child notification is-info">
          <div class="content">
            <p>Here is your new URL:</p>
            <div class="field" style="width: 100%">
              <div class="control is-expanded">
                <input class="input" value="<%= url_url(@conn, :redirect_shortcode, @url.shortcode) %>" onclick="this.setSelectionRange(0, this.value.length)" />
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

